template<class T>
class ArrayManager
{
public:

    ArrayManager(unsigned size)
    {
        
    }
    //T* AddElement(const T& element );

private:

    unsigned mSize;
    T* mArray;
};